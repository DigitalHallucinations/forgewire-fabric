# fw-streams — per-task stream sequence counters

[package]
name = "fw-streams"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
parking_lot = "0.12"

[lints]
workspace = true
